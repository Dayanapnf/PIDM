import { StyleSheet } from "react-native";

const styleButtonAdd = StyleSheet.create({
    container:{
        width:60,
        height:60,
        borderRadius:30,
        backgroundColor: '#78C2A4',
        alignItems: 'center',
        justifyContent: 'center',
        marginBottom: 20,

    },
})

export default styleButtonAdd